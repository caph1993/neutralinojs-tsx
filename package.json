{
  "scripts": {
    "bundle": "webpack -p --config webpack.config.js && cp -r -v ./src/static/* ./resources",
    "build": "npm run bundle && neu build",
    "start": "npm run bundle && neu run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "typescript": "^3.9.7",
    "@types/react": "^17.0.30",
    "@types/react-dom": "^17.0.9",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12",
    "css-loader": "^3.6.0",
    "ts-loader": "^6.2.2",
    "extract-text-webpack-plugin": "^3.0.2",
    "mini-css-extract-plugin": "^0.9.0"
  }
}
